---
import PageTitle from "@/components/layout/PageTitle.astro";
import Pagination from "@/components/Pagination.astro";

import SiteLayout from "@/layouts/SiteLayout.astro";

import { ALL_MODELS, Collection } from "@/content.config";
import type { Page } from "astro/dist/types/public";
import AssetCard from "@/components/AssetCard.astro";
import SectionTitle from "@/components/ui/SectionTitle.astro";
import { findContent } from "@/scripts/utilities";

interface Props {
	page: Page<any>;
}

export async function getStaticPaths({ paginate }: any): Promise<any> {
	return paginate(ALL_MODELS, {
		pageSize: 6,
	});
}

const { page } = Astro.props;
const isCurrentPage = page.url.current === "/models";
---

<SiteLayout pageTitle="Models">
	<PageTitle heading="Models" />
	{isCurrentPage && (
		<section class="column-md">
			<SectionTitle
				heading="Artist's Pick"
				badgeLabel="FEATURED"
				badgeStyle="alt"
			/>
			<AssetCard content={findContent(ALL_MODELS, "poltergeist")} />
		</section>
	)}
	<section class="column-md">
		<SectionTitle
			heading={
				isCurrentPage
					? "All Models"
					: `Page ${page.url.current.split("/").pop()} of ${page.lastPage}`
			}
		/>
		<div class="grid-md">
			{page.data.map((model: Collection) => <AssetCard content={model} />)}
		</div>
	</section>
	<Pagination {page} content={ALL_MODELS} />
</SiteLayout>
