---
layout: "/layouts/base.njk"
---
<header >
    <h1>   
        {% if buildName %}{{buildName}} - {% endif %}{{title}}
    </h1>
    <span>{{content | timeToRead}} read &bullet; Posted on: {{page.date | postDate}}</span>
</header>
{{content | safe}}
{% if buildURL %}
    <a href="{{buildURL}}"><< View build</a>
{% endif %}
