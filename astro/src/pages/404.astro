---
import ContentWrapper from "@components/layout/ContentWrapper.astro";
import Container from "../layouts/Container.astro";
import GlobalGrid from "../layouts/GlobalGrid.astro";
import GlobalLayout from "../layouts/GlobalLayout.astro";
import Element from "@components/Element.astro";
---

<GlobalLayout title="404 Error">
    <GlobalGrid class="site-grid">
        <Container layout="content">
            <h1>404 Error</h1>
            <!-- <p class="lead">{subtitle}</p> -->
            <div class="flex gap-2">
                <!-- {ctas?.map((cta) => <Link data={cta} />)} -->
            </div>
        </Container>
    </GlobalGrid>
    <ContentWrapper>
        <Container layout="content" class="grid-span-content">
            <Element>content</Element>
        </Container>
    </ContentWrapper>
</GlobalLayout>
